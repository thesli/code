// Generated by CoffeeScript 1.6.3
(function() {
  angular.module("ft.mobile", ['ionic', 'monospaced.qrcode', 'ft.mobile.route']).controller("sideMenuCtrl", function($scope, $rootScope) {
    console.log("sideMenuCtrl");
    $rootScope.toggleLeft = function() {
      return $scope.sideMenuController.toggleLeft();
    };
    return $rootScope.toggleRight = function() {
      return $scope.sideMenuController.toggleRight();
    };
  }).controller("tabsCtrl", function($scope, $rootScope) {
    console.log("tabsCtrl");
    $scope.toggleLeft = function() {
      return $rootScope.toggleLeft();
    };
    return $scope.toggleRight = function() {
      return $rootScope.toggleRight();
    };
  }).controller("qrcodeCtrl", [
    "$scope", "$rootScope", "$http", function($scope, $rootScope, $http) {
      $scope.qrSuccess = false;
      return $scope.testRest = function() {
        return $http.get("/qrcode").success(function(data) {
          $scope.qrSuccess = true;
          return $scope.test = data;
        });
      };
    }
  ]).run([
    "$rootScope", "$timeout", function($rootScope, $timeout) {
      $rootScope.checkoutBtnPos = document.querySelector("[ng-click='toggleRight()']").getBoundingClientRect();
      console.log($rootScope.checkoutBtnPos);
      return $rootScope.amount = 0;
    }
  ]);

  angular.module("ft.mobile.route", ["ngRoute", "ngAnimate"]).config([
    "$routeProvider", function($routeProvider) {
      return $routeProvider.when("/", {
        template: "<h1>HELLOWORLD HERE</h1><Button ng-click='try()'>{{test}}</Button><button ng-click='ani()'>ani()</button>",
        controller: [
          "$scope", "$rootScope", "$animate", function($scope, $rootScope, $animate) {
            var aniObj;
            $scope.test = "fuck you";
            $scope["try"] = function() {
              return console.log($rootScope.checkoutBtnPos);
            };
            aniObj = document.querySelector("[ng-click='ani()']");
            return $scope.ani = function() {
              return $animate.enter(aniObj, ".moveToCheckout");
            };
          }
        ]
      });
    }
  ]).animation(".moveToCheckout", function() {
    return {
      enter: function(element, done) {
        console.log("enter");
        return console.log(element);
      },
      leave: function() {
        return console.log("leave");
      }
    };
  });

}).call(this);

//# sourceMappingURL=app.map
